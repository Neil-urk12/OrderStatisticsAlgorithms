def median_of_medians(arr: list[int], k: int) -> int:
    """
    Find the k-th smalles element in arr using the Median of Medians algorithm.

    Parameters:
        arr : list[int] : the input list of numbers
        k : int : the index of the element to find (0-based index)
    """
